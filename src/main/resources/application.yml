server:
  port: 8888

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/expressbank
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
